<h1>Inbox</h1>

<div class="panel-body">
	<table class="table">
		<thead>
			<th>Sender</th>
			<th>Message</th>
		</thead>
		<tbody>
			<% @mail_messages.each do |mail_message| %>
				<tr>
					<td><%= mail_message.sender.email %></td>
					<td><%= mail_message.text %></td>
				</tr>
			<% end %>
		</tbody>
	</table>
</div>